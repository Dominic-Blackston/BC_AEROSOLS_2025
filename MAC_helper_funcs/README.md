Code to run: 
1. Datetracker.ipynb: just change path_to_track. Used to track the dates for each campaign and the rows for each date per campaign. 

2. MAC_count_rows.ipynb: change df. Used to count rows per organization per campaign. 

3. MAC_drop_campaigns.ipynb: change PATH and OUTPUT_PATH and campaigns_to_drop. Drop certain campaigns that have low counts. We mainly use it to drop the NAAMES campaigns. 

4. MAC_find_avg_cols.ipynb: change cols_to_avg and df. Finds the average of all the columns. 
